# Development environment secrets
secretGenerator:
  - name: laravel-secrets
    files:
      - .env  # All development environment secrets (Laravel + MySQL)
    type: Opaque
generatorOptions:
  disableNameSuffixHash: true

# Note: For production, use sealed secrets in overlays/prod/ 