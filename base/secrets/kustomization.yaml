# Base configuration uses sealed secrets for GitOps
resources:
  - laravel-secret-sealed.yaml

# Note: Development environment overrides this with .env file in overlays/dev/
# Note: For production, use sealed secrets in overlays/prod/ 